# ============================================================================
# AmsirarTrip Environment Configuration Template
# ============================================================================
# 
# IMPORTANT: Copy this file to .env.local (NOT .env) for development
# 
#   cp .env.example .env.local
#
# Then fill in your actual values. Never commit .env.local to version control!
#
# For production deployments, set these as environment variables in your
# hosting platform (Vercel, Netlify, etc.) - do NOT use .env files in production.
#
# ============================================================================

# ----------------------------------------------------------------------------
# reCAPTCHA Configuration
# ----------------------------------------------------------------------------
# Get your keys at: https://www.google.com/recaptcha/admin
#
# reCAPTCHA v2 (Checkbox CAPTCHA) - Used in booking/contact forms
# These keys are PUBLIC (NEXT_PUBLIC_*) and will be visible in browser

NEXT_PUBLIC_RECAPTCHA_SITE_KEY=your_recaptcha_v2_site_key_here
RECAPTCHA_SECRET_KEY=your_recaptcha_v2_secret_key_here

# reCAPTCHA v3 (Invisible/Score-based) - Used in newsletter subscription
# v3 provides better UX with background bot detection

NEXT_PUBLIC_RECAPTCHA_V3_SITE_KEY=your_recaptcha_v3_site_key_here
RECAPTCHA_V3_SECRET_KEY=your_recaptcha_v3_secret_key_here

# ----------------------------------------------------------------------------
# Email Configuration (Gmail SMTP)
# ----------------------------------------------------------------------------
# SECURITY WARNING: Use App Passwords, NOT your regular Gmail password!
#
# Setup Instructions:
# 1. Enable 2-Factor Authentication on your Google Account
#    https://myaccount.google.com/security
#
# 2. Generate an App Password:
#    https://myaccount.google.com/apppasswords
#    - Select "Mail" as the app
#    - Select "Other" as the device and name it "AmsirarTrip"
#    - Copy the 16-character password (no spaces)
#
# 3. Use the App Password below, NOT your regular password
#
# ALTERNATIVE: For better security, consider OAuth2 authentication
# See: EMAIL-OAUTH2-SETUP.md for OAuth2 configuration guide
# ----------------------------------------------------------------------------

# Gmail account to send emails FROM (must have App Password enabled)
GMAIL_USER=your-email@gmail.com

# Gmail App Password (16 characters, generated from Google Account settings)
# NEVER use your actual Gmail password here!
GMAIL_PASS=abcd efgh ijkl mnop

# Email address to receive form submissions TO (optional, defaults to GMAIL_USER)
MAIL_TO=recipient@example.com

# ----------------------------------------------------------------------------
# Health Check Protection (Optional)
# ----------------------------------------------------------------------------
# Protect /api/health endpoint with a secret token
# Usage: GET /api/health?token=YOUR_TOKEN
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

HEALTH_CHECK_TOKEN=your-random-secure-token-here

# ----------------------------------------------------------------------------
# CSRF Protection (Optional)
# ----------------------------------------------------------------------------
# Generate a random secret for CSRF token signing (production only)
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

CSRF_SECRET=your-random-32-byte-hex-string-here

# ----------------------------------------------------------------------------
# Monitoring and Logging (Optional - for production error tracking)
# ----------------------------------------------------------------------------
# Sentry Configuration - Get your DSN at: https://sentry.io/
# After installing @sentry/nextjs, uncomment these:

# NEXT_PUBLIC_SENTRY_DSN=https://your-key@sentry.io/your-project-id
# SENTRY_AUTH_TOKEN=your-sentry-auth-token
# SENTRY_ORG=your-organization
# SENTRY_PROJECT=your-project

# Security Alert Webhook (Slack/Discord) - for real-time security notifications
# Get webhook URL from: Slack > Apps > Incoming Webhooks

# SECURITY_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# ============================================================================
# Development vs Production Notes
# ============================================================================
#
# DEVELOPMENT:
#   - Copy this file to .env.local
#   - .env.local is automatically loaded by Next.js
#   - .env.local is git-ignored (never committed)
#
# PRODUCTION:
#   - Set these as environment variables in your hosting platform
#   - Vercel: Project Settings > Environment Variables
#   - Netlify: Site Settings > Build & Deploy > Environment
#   - Do NOT deploy .env files to production
#
# SECURITY CHECKLIST:
#   ✅ .env.local is in .gitignore
#   ✅ Using Gmail App Password (not regular password)
#   ✅ Never commit real keys to version control
#   ✅ Rotate keys if accidentally exposed
#   ✅ Use different keys for development and production
#
# ============================================================================
